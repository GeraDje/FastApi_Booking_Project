stages:
  - build
  - deploy

build-job:
  stage: build
  script:
     - docker build -t booking-api-images .


deploy-job:
  stage: deploy
  script:
    - docker compose up -d

default:
  before_script:
    - cp "$ENV" .env - cp "$ENV_TEST" .env.test